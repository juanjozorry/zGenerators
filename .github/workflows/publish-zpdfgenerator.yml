name: Publish zPdfGenerator to NuGet

on:
  push:
    tags:
      - 'zPdfGenerator-v*'

jobs:
  publish-core:
    runs-on: ubuntu-latest

    env:
      PROJECT_PATH: src/zPdfGenerator/zPdfGenerator.csproj
      NUGET_SOURCE: https://api.nuget.org/v3/index.json

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x' 

      - name: Extract version from tag
        id: version
        run: |
          # github.ref_name = zPdfGenerator-v1.2.3
          TAG_NAME="${GITHUB_REF_NAME}"
          VERSION="${TAG_NAME#zPdfGenerator-v}"
          BASE_VERSION="${VERSION%%-*}"
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "base_version=$BASE_VERSION" >> $GITHUB_OUTPUT

      - name: Restore
        run: dotnet restore $PROJECT_PATH

      - name: Build
        run: |
          dotnet build $PROJECT_PATH \
            --configuration Release --no-restore \
            -p:Version=${{ steps.version.outputs.version }} \
            -p:AssemblyVersion=${{ steps.version.outputs.base_version }} \
            -p:FileVersion=${{ steps.version.outputs.base_version }}

      - name: Pack
        run: |
          dotnet pack $PROJECT_PATH \
            --configuration Release \
            --no-build \
            -p:PackageVersion=${{ steps.version.outputs.version }} \
            -p:Version=${{ steps.version.outputs.version }} \
            -p:AssemblyVersion=${{ steps.version.outputs.base_version }} \
            -p:FileVersion=${{ steps.version.outputs.base_version }} \
            -o ./artifacts/core

      - name: Push to NuGet
        run: | 
          dotnet nuget push "./artifacts/core/*.nupkg" \
            --api-key "${{ secrets.NUGET_API_KEY }}" \
            --source "$NUGET_SOURCE" \
            --skip-duplicate

  publish-tools:
    needs: publish-core
    runs-on: ubuntu-latest

    env:
      PROJECT_PATH: src/zPdfGenerator.TemplatePreview/zPdfGenerator.TemplatePreview.csproj
      NUGET_SOURCE: https://api.nuget.org/v3/index.json

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Derive version from tag
        id: ver
        shell: bash
        run: |
          TAG_NAME="${GITHUB_REF_NAME}"
          VERSION="${TAG_NAME#zPdfGenerator-v}"
          BASE_VERSION="${VERSION%%-*}"
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "base_version=$BASE_VERSION" >> $GITHUB_OUTPUT

      - name: Restore
        run: dotnet restore $PROJECT_PATH

      - name: Build
        run: |
          dotnet build $PROJECT_PATH \
            -c Release --no-restore \
            -p:Version=${{ steps.ver.outputs.version }} \
            -p:AssemblyVersion=${{ steps.ver.outputs.base_version }} \
            -p:FileVersion=${{ steps.ver.outputs.base_version }}

      - name: Pack tools (force PackageReference)
        run: |
          dotnet pack $PROJECT_PATH \
            -c Release \
            --no-build \
            -p:PackageVersion=${{ steps.ver.outputs.version }} \
            -p:Version=${{ steps.ver.outputs.version }} \
            -p:AssemblyVersion=${{ steps.ver.outputs.base_version }} \
            -p:FileVersion=${{ steps.ver.outputs.base_version }} \
            -p:UseProjectRefs=false \
            -p:ZPdfGeneratorVersion=${{ steps.ver.outputs.version }} \
            -o ./artifacts/tools

      - name: Push tools to NuGet.org
        run: |
          dotnet nuget push "./artifacts/tools/*.nupkg" \
            --api-key "${{ secrets.NUGET_API_KEY }}" \
            --source "$NUGET_SOURCE" \
            --skip-duplicate

  publish-charts:
    needs: publish-core
    runs-on: ubuntu-latest

    env:
      PROJECT_PATH: src/zPdfGenerator.Charts/zPdfGenerator.Charts.csproj
      NUGET_SOURCE: https://api.nuget.org/v3/index.json

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Derive version from tag
        id: ver
        shell: bash
        run: |
          TAG_NAME="${GITHUB_REF_NAME}"
          VERSION="${TAG_NAME#zPdfGenerator-v}"
          BASE_VERSION="${VERSION%%-*}"
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "base_version=$BASE_VERSION" >> $GITHUB_OUTPUT

      - name: Restore
        run: dotnet restore $PROJECT_PATH

      - name: Build
        run: |
          dotnet build $PROJECT_PATH \
            -c Release --no-restore \
            -p:Version=${{ steps.ver.outputs.version }} \
            -p:AssemblyVersion=${{ steps.ver.outputs.base_version }} \
            -p:FileVersion=${{ steps.ver.outputs.base_version }}

      - name: Pack charts (force PackageReference)
        run: |
          dotnet pack $PROJECT_PATH \
            -c Release \
            --no-build \
            -p:PackageVersion=${{ steps.ver.outputs.version }} \
            -p:Version=${{ steps.ver.outputs.version }} \
            -p:AssemblyVersion=${{ steps.ver.outputs.base_version }} \
            -p:FileVersion=${{ steps.ver.outputs.base_version }} \
            -p:UseProjectRefs=false \
            -p:ZPdfGeneratorVersion=${{ steps.ver.outputs.version }} \
            -o ./artifacts/charts

      - name: Push charts to NuGet.org
        run: |
          dotnet nuget push "./artifacts/charts/*.nupkg" \
            --api-key "${{ secrets.NUGET_API_KEY }}" \
            --source "$NUGET_SOURCE" \
            --skip-duplicate
